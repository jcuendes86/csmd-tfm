/**
 * Este fichero se encarga de inicializar la configuración de las variables de entorno
 * antes de que se inicie el servidor.
 */
import EnvConfig from './server/config/envs';

// Inicializar la configuración de las variables de entorno.
EnvConfig.init();